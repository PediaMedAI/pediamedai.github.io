---
title: "Updates"
description: "Latest updates from PediaMed AI."
# The minimal single-entry mirror (amilabs.xyz/updates style), rendered
# by the standalone layouts/updates/list.html. Reads the single latest
# entry from data/news.yaml, so it auto-updates on every deploy. Public
# and indexable; reachable from the shared header's "Updates" link.
---
